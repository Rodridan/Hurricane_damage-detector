from setuptools import setup, find_packages

setup(
    name="hurricane-damage-detector",
    version="0.1.0",
    packages=["damage_detector"],  # Only package this
    install_requires=[
        # (Add your dependencies here or rely on requirements.txt)
    ],
)
